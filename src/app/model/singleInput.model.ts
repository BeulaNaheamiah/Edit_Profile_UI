import { SingleChoice } from './SingleChoice.model';

export class SingleInput{
    ethnicity:SingleChoice[];
    gender:SingleChoice[];
    religion:SingleChoice[];
    figure:SingleChoice[];
    marital_status:SingleChoice[];
}